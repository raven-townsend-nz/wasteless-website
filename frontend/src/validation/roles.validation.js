/**
 * Defines the user roles:
 * - User
 * - Global Application Admin (GAA)
 * - Default Global Application Admin (DGAA)
 */

const USER = "user";
const GLOBAL_APPLICATION_ADMIN = "global_admin";
const DEFAULT_GLOBAL_APPLICATION_ADMIN = "default_global_admin";

export default {
    USER,
    GLOBAL_APPLICATION_ADMIN,
    DEFAULT_GLOBAL_APPLICATION_ADMIN
};